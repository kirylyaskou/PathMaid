{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop updater and restart permissions for the main window",
  "windows": ["main"],
  "platforms": ["windows", "macOS", "linux"],
  "permissions": ["updater:default", "process:allow-restart"]
}
