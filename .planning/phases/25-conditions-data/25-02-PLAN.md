# Plan 25-02: shared/api/conditions.ts

**Phase:** 25 — Conditions Data Pipeline
**Requirements:** COND-01, COND-02
**Status:** Complete

## Goal

Frontend API layer for conditions — search, slug lookup, all conditions, count.

## Changes

- `src/shared/api/conditions.ts` (new):
  - `ConditionRow` interface — mirrors `conditions` table columns
  - `getAllConditions()` — SELECT * FROM conditions ORDER BY name ASC
  - `searchConditions(query)` — LIKE search on name (conditions are ~50 total, no FTS5 needed)
  - `getConditionBySlug(slug)` — single condition by slug
  - `getConditionsByGroup(groupName)` — filter by group_name

- `src/shared/api/index.ts` — add `export * from './conditions'`

## Notes

- No FTS5 for conditions — only ~50 total, simple LIKE query is sufficient
- `ConditionRow` is also needed in `entities/condition/model/types.ts` for badge integration
- getConditionBySlug used by badge popover in Phase 27
