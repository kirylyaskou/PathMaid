// Placeholder — full list page implemented in Task 2.
export function CustomCreaturesListPage() {
  return (
    <div className="flex-1 flex items-center justify-center p-8">
      <p className="text-sm text-muted-foreground">Custom Creatures list — pending.</p>
    </div>
  )
}
